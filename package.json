{
  "name": "a-frame_learn",
  "version": "1.0.0",
  "description": "my A-Frame Scene!For Psychology",
  "scripts": {
    "fastapi:dev": "uvicorn backend.main:app --reload --host 0.0.0.0 --port 8000",
    "fastapi:prod": "uvicorn backend.main:app --host 0.0.0.0 --port 8000",
    "aframe": "cross-env NODE_ENV=development snowpack dev --no-hmr",
    "build": "cross-env NODE_ENV=production snowpack build",
    "serve": "http-server build -c-1 -p 80 --404 _404.html",
    "start:dev": "pnpm run aframe && pnpm run fastapi:dev",
    "start:prod": "cross-env NODE_ENV=production pnpm run build && pnpm run prod && pnpm run fastapi:dev"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Schweik7/A-Frame_learn.git"
  },
  "keywords": [
    "aframe",
    "js",
    "ts"
  ],
  "author": "Schweik7",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/Schweik7/A-Frame_learn/issues"
  },
  "homepage": "https://github.com/Schweik7/A-Frame_learn#readme",
  "devDependencies": {
    "concurrently": "^8.2.2",
    "cross-env": "^7.0.3",
    "snowpack": "^3.8.8",
    "webpack": "^5.91.0",
    "webpack-cli": "^5.1.4",
    "webpack-dev-server": "^5.0.4"
  },
  "dependencies": {
    "@parcel/runtime-browser-hmr": "^2.12.0",
    "dotenv-flow": "^4.1.0",
    "serve": "^14.2.3"
  }
}
